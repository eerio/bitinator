RewriteEngine On

RewriteCond %{HTTPS} =on
RewriteRule ^(.*)$ - [env=protocol:https]

RewriteCond %{HTTPS} !=on
RewriteRule ^(.*)$ - [env=protocol:http]

RewriteCond %{HTTPS} !www.bitinator.net$ [NC]
RewriteRule ^(.*)$ ${ENV:protocol}://www.bitinator.net/$1 [L,R=301]

